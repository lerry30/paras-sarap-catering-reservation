'use client';
import { useEffect } from 'react';

const Admin = ({ }) => {
    const start = () => {
        
    }

    useEffect(() => {
        start();
    });

    return (
        <section>
            
        </section>
    );
}

export default Admin;