'use client';

const { Suspense } = require('react');

const ViewWeddingThemes = () => {
    return <Suspense fallback={ <Loading customStyle="size-full" /> }>
        
    </Suspense>
}

export default ViewWeddingThemes;